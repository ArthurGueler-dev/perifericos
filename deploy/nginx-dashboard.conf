server {
    listen 80;
    server_name ti.in9automacao.com.br;
    root /var/www/perifericos-dashboard;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /static {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
